import dirTree from "directory-tree";

// console.log(dirTree);
// const tree = dirTree("./bCya-IDE");
export default function FileBrowser() {
    // console.log(dirTree(""));
    return (
        <div className={' h-full'}>
            <button className={'text-white'} style={{fontFamily: 'Montserrat'}}>Hellow</button>
        </div>
    );
}